```
---
description: Building Instafix and Instafix-llvm
---

# Building instafix (located: `~/src/instafix/`)
make sure to run `source ~/src/instafix/init_dev_instafix_env.sh` first
you just have to run `cmake --workflow --preset linux`

# Building instafix-llvm (locatd: `~/src/instafix-llvm/llvm`)
when you need to build instafix-llvm, 
you just have to run `cmake --workflow --preset linux`
```
