---
phase: 02-remote-tmux-sessions
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/session.rs
  - src/lib.rs
autonomous: true
requirements:
  - SESS-03
  - SESS-04

must_haves:
  truths:
    - "Bundle and workspace names containing dots or colons produce valid tmux session names with hyphens"
    - "SSH commands sent to remote hosts always allocate a TTY so tmux can function"
    - "Remote session commands use atomic attach-or-create so re-running never duplicates sessions"
  artifacts:
    - path: "src/session.rs"
      provides: "Session name sanitization and SSH/tmux command builder"
      exports: ["sanitize_session_name", "build_remote_session_cmd"]
      min_lines: 40
    - path: "src/lib.rs"
      provides: "Module export for session"
      contains: "pub mod session"
  key_links:
    - from: "src/session.rs"
      to: "src/main.rs"
      via: "main.rs imports session module to route remote workspaces and bundles"
      pattern: "session::build_remote_session_cmd"
---

<objective>
Create the session module with two pure functions: session name sanitization and remote SSH/tmux command building.

Purpose: These are the building blocks for Phase 2 remote sessions. Sanitization ensures tmux-compatible session names (SESS-03). The command builder constructs the `ssh -t host "tmux new-session -A -s name"` string with proper TTY allocation (SESS-04).

Output: `src/session.rs` with `sanitize_session_name()` and `build_remote_session_cmd()`, fully tested via TDD.
</objective>

<execution_context>
@/Users/juliusalexandre/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliusalexandre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-remote-tmux-sessions/02-RESEARCH.md
@src/lib.rs
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Session name sanitization and remote command builder</name>
  <files>src/session.rs, src/lib.rs</files>
  <action>
    TDD cycle for two pure functions in a new session module.

    RED phase — write failing tests in src/session.rs (#[cfg(test)] mod tests):

    sanitize_session_name tests:
    - "dev.frontend" -> "dev-frontend" (dots to hyphens)
    - "servers:prod" -> "servers-prod" (colons to hyphens)
    - "my.workspace:test!" -> "my-workspace-test" (special chars stripped, trailing hyphens trimmed)
    - "simple" -> "simple" (alphanumeric unchanged)
    - "a_b-c" -> "a_b-c" (underscores and hyphens preserved)
    - "--leading" -> "leading" (leading hyphens trimmed)
    - "" -> "" (empty string stays empty)
    - "a..b" -> "a--b" (consecutive dots become consecutive hyphens -- acceptable)

    build_remote_session_cmd tests:
    - ("host", "myws", None) -> "ssh -t host \"tmux new-session -A -s myws\""
    - ("user@server", "dev.app", Some("~/src")) -> "ssh -t user@server \"cd ~/src && tmux new-session -A -s dev-app\""
    - Always includes -t flag (TTY allocation, SESS-04)
    - Always uses new-session -A -s (atomic attach-or-create)
    - Session name is sanitized before embedding in command

    GREEN phase — implement:
    1. Create src/session.rs with two public functions:
       - `pub fn sanitize_session_name(name: &str) -> String` -- Map dots and colons to hyphens, strip other non-alphanumeric/hyphen/underscore chars, trim leading/trailing hyphens.
       - `pub fn build_remote_session_cmd(host: &str, session_name: &str, dir: Option<&str>) -> String` -- Build the SSH command string. Calls sanitize_session_name internally. With dir: `ssh -t {host} "cd {dir} && tmux new-session -A -s {sanitized}"`. Without dir: `ssh -t {host} "tmux new-session -A -s {sanitized}"`.
    2. Add `pub mod session;` to src/lib.rs (after the existing `pub mod ssh;` line).
    3. Tests go in #[cfg(test)] mod tests inside src/session.rs (Rust convention -- tests beside code).

    REFACTOR phase — clean up if needed, ensure no clippy warnings.
  </action>
  <verify>
    <automated>cargo test -- session && cargo clippy -- -D warnings</automated>
  </verify>
  <done>
    - All sanitization test cases pass (8 cases)
    - All command builder test cases pass (2+ cases covering with/without dir)
    - cargo build succeeds with no warnings
    - src/lib.rs exports pub mod session
  </done>
</task>

</tasks>

<verification>
- `cargo test` passes with all session module tests green
- `cargo build` compiles without warnings
</verification>

<success_criteria>
- src/session.rs exists with sanitize_session_name and build_remote_session_cmd
- All test cases from the behavior section pass
- src/lib.rs exports the session module
- cargo build succeeds with no warnings
</success_criteria>

<output>
After completion, create `.planning/phases/02-remote-tmux-sessions/02-01-SUMMARY.md`
</output>
